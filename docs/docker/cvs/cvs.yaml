services:
  os-jel:
    # Overriden by environment specific YAML configurations (e.g. docker-compose.dev.yaml)
    # image: crisalidesr/crisalid-vocab-search:os-jel-latest
    expose:
      - "9200"
    # only if you want to access from host machine
    #ports: [ "9200:9200" ]
    networks:
      - crisalid-vocab-search-network
    profiles:
      - cvs

  os-acm:
    # Overriden by environment specific YAML configurations (e.g. docker-compose.dev.yaml)
    # image: crisalidesr/crisalid-vocab-search:os-acm-latest
    expose:
      - "9200"
    # only if you want to access from host machine
    #ports: [ "9200:9200" ]
    networks:
      - crisalid-vocab-search-network
    profiles:
      - cvs

  os-aat:
    # Overriden by environment specific YAML configurations (e.g. docker-compose.dev.yaml)
    # image: crisalidesr/crisalid-vocab-search:os-aat-latest
    expose:
      - "9200"
    # only if you want to access from host machine
    #ports: [ "9200:9200" ]
    networks:
      - crisalid-vocab-search-network
    profiles:
      - cvs

  os-elsst:
    # Overriden by environment specific YAML configurations (e.g. docker-compose.dev.yaml)
    # image: crisalidesr/crisalid-vocab-search:os-elsst-latest
    expose:
      - "9200"
    # only if you want to access from host machine
    #ports: [ "9200:9200" ]
    networks:
      - crisalid-vocab-search-network
    profiles:
      - cvs

  os-pactols:
    # Overriden by environment specific YAML configurations (e.g. docker-compose.dev.yaml)
    # image: crisalidesr/crisalid-vocab-search:os-pactols-latest
    expose:
      - "9200"
    # only if you want to access from host machine
    #ports: [ "9200:9200" ]
    networks:
      - crisalid-vocab-search-network
    profiles:
      - cvs

  os-euroscivoc:
    # Overriden by environment specific YAML configurations (e.g. docker-compose.dev.yaml)
    # image: crisalidesr/crisalid-vocab-search:os-euroscivoc-latest
    expose:
      - "9200"
    # only if you want to access from host machine
    #ports: [ "9200:9200" ]
    networks:
      - crisalid-vocab-search-network
    profiles:
      - cvs

  cvs-api:
    # Overriden by environment specific YAML configurations (e.g. docker-compose.dev.yaml)
    # image: crisalidesr/crisalid-vocab-search:api-1.0
    ports: [ "${CVS_API_PORT:-8000}:8000" ]
    environment:
      API_VERSION: ${API_VERSION:-v1}
    depends_on: [ os-jel , os-acm , os-aat ]
    networks:
      - crisalid-vocab-search-network
      - svp-network
    profiles:
      - cvs

networks:
  crisalid-vocab-search-network:
    driver: bridge
  svp-network:
    driver: bridge
